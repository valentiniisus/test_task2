[pytest]
pythonpath = .
testpaths = tests
markers =
    api: API tests (requests against dummyjson.com + local fixture backend)
    e2e: browser tests (Playwright against the-internet.herokuapp.com)
    unit: unit tests for framework helpers, no network
addopts = -ra --strict-markers
