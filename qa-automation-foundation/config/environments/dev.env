# Local/dev environment.
# Public demo services don't expose separate dev/staging/prod hosts,
# so all three env files point at the same public endpoints here.
# In a real project each file would hold a different host + its own secrets.
API_BASE_URL=https://dummyjson.com
CRUD_API_BASE_URL=http://localhost:3001
UI_BASE_URL=https://the-internet.herokuapp.com
UI_LOGIN=tomsmith
UI_PASSWORD=SuperSecretPassword!
API_TIMEOUT_MS=5000
UI_TIMEOUT_MS=15000
