# Staging environment config.
# Same public hosts as dev for this demo repo — swap for real staging
# hosts/credentials once the project has an actual staging deployment.
API_BASE_URL=https://dummyjson.com
CRUD_API_BASE_URL=http://localhost:3001
UI_BASE_URL=https://the-internet.herokuapp.com
UI_LOGIN=tomsmith
UI_PASSWORD=SuperSecretPassword!
API_TIMEOUT_MS=8000
UI_TIMEOUT_MS=20000
