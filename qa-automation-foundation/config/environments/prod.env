# Prod-like environment config.
# Kept read-only in mindset: no destructive/write tests should target this
# file's host in a real setup. Included so the switching mechanism (TEST_ENV)
# is demonstrated end to end, per the task requirement.
API_BASE_URL=https://dummyjson.com
CRUD_API_BASE_URL=http://localhost:3001
UI_BASE_URL=https://the-internet.herokuapp.com
UI_LOGIN=tomsmith
UI_PASSWORD=SuperSecretPassword!
API_TIMEOUT_MS=10000
UI_TIMEOUT_MS=25000
